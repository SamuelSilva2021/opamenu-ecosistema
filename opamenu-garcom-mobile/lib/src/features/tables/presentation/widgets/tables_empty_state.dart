import 'package:flutter/material.dart';

class TablesEmptyState extends StatelessWidget {
  const TablesEmptyState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Nenhuma mesa encontrada'),
    );
  }
}

